Drop your photo here as: photo.jpg (or photo.png — then also update the src path in
src/components/Hero.jsx, ProfilePhoto component, to /profile/photo.png)

Recommended: a square, front-facing photo, at least 400x400px, well-lit, plain/simple
background. It will be cropped into a circle automatically.

Until a real photo is added, the site shows a "BM" initials placeholder instead —
nothing will break either way.
